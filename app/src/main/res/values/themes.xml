<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GestionDeTramitesMobile" parent="Theme.MaterialComponents.DayNight.DarkActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primaryTheme</item>
        <item name="colorPrimaryVariant">@color/primaryDarkTheme</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondaryTheme</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Customize your theme here. -->
    </style>
    <style name="Theme.GestionDeTramitesMobile.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="PrimaryButton">
        <item name="colorPrimary">@color/secondaryTheme</item>
    </style>
    <style name="SecondaryButton">
        <item name="colorPrimary">@color/infoTheme</item>
    </style>
    <style name="ErrorButton">
        <item name="colorPrimary">@color/errorTheme</item>
    </style>
</resources>